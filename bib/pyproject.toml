[tool.ruff]
line-length = 88
target-version = "py311"

# какие правила проверять
[tool.ruff.lint]
select = ["E", "F", "I", "B"]

# E = style errors (pep8)
# F = логические ошибки (pyflakes)
# I = imports
# B = потенциальные баги